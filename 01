#include <stdio.h>
#include <stdlib.h>

int main(){
    int a, b, c;

    printf("input the second :\n");
    scanf("%d", &a);

    b = a / 60;
    c = a % 60;

    printf("the time is %d:%d\n", b, c);

    return 0;
}